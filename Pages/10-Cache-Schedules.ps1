#define common schedules

$udSchedule1min = New-UDEndpointSchedule -Every 1 -Minute

$udSchedule5min = New-UDEndpointSchedule -Every 5 -Minute

$udSchedule30min = New-UDEndpointSchedule -Every 30 -Minute

